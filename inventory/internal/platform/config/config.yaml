http_port: "8082"

kafka_brokers:
  - "localhost:9092"
kafka_client_id: "inventory-service"
kafka_group_id: "inventory-service-v1"

topic_order_events: "saga.order.events"
topic_payment_events: "saga.payment.events"
topic_inventory_events: "saga.inventory.events"
topic_commands: "saga.commands"
topic_dlq: "saga.dlq"

database_dsn: "postgres://inventory:inventory@localhost:5434/inventory?sslmode=disable"
database_connect_timeout: 10s
shutdown_timeout: 5s
